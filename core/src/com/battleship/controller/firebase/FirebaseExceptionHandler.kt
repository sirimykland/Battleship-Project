package com.battleship.controller.firebase

import com.battleship.GSM
import com.battleship.controller.state.GuiState

class FirebaseExceptionHandler(private val code: String, private val message: String){

    /* init{
        if (GSM.peek() is GuiState) GSM.peek().addGuiError(code, message)
    }*/
}